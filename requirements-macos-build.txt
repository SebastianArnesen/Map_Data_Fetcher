# macOS release builds: Qt 6.10+ (PySide6 >= 6.10) requires macOS 13 Ventura+.
# Pin Qt 6.9 so frozen .app bundles run on macOS 12 Monterey (and later).
PySide6>=6.8.0,<6.10.0
requests>=2.33.0
pyproj>=3.6.0
certifi>=2024.0.0
